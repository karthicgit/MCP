# MCP
This repo contains sample code for MCP servers.
1. **CloudAdvisor MCP**:
   
     This MCP is used to list the resources which falls under cost category in cloud advisor . It list only the resources whose estimated savings is greater than zero .
   
     It uses resource principal for access. Create least privilege IAM policy.
